# Production build config (used by `vite build`, e.g. the bundled desktop exe).
# The desktop build runs Seedance-only (no LLM CLIs on the target machine), so
# the forced AI-provider setup gate is bypassed. Loaded automatically by Vite
# for production builds; the dev server uses .env.local instead.
VITE_DISABLE_SETUP_GATE=1
