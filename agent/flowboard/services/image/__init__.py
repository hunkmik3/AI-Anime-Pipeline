"""Image services (review). Minimal package init — only the self-contained
``atrium_upscale`` module is used here (the batch-4K upscaler). The provider
abstraction (base/registry/flow/flux) from prod is intentionally NOT imported so
this package has no extra dependencies."""
